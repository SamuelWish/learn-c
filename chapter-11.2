#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <clocale>
#include <string.h>
#include <ctype.h>
#define SIZE 10
#define RESULT "Вот что получилось сохранить:"

void getSymbol(char arr [], int n);

int main(void)
{	setlocale(LC_ALL, "Russian"); //Добавим русский язык
	char word[SIZE];
	
	printf("Введите не более %d символов (включая пробелы, символы табуляции и новой строки):\n", SIZE - 1);
	getSymbol(word, SIZE-1);
	
	return 0;
}
void getSymbol(char arr [], int n)
{
	int i;
	char ch;
	for (i = 0; i < n; i++)
	{
		ch = getchar();
		if (ch != ' ' && ch!= '\n' && ch != '	')
			arr[i] = ch;
		else
			break;
	}
	arr[i] = '\0';
	puts(RESULT);
	puts(arr);
}
